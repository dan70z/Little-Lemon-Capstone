const directory = new Map();
directory.set('homepage', {name: 'Home', path: '/', anchorable: true});
directory.set('reservations', {name: 'Reserve a Table', path:'/reservations'});
export default directory;